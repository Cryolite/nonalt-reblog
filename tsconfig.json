{
  "compilerOptions": {
    "module": "commonjs",
    "target": "es2022",
    "strict": true,
    // TODO: Remove allowJs once we finish TS migration.
    "allowJs": true,
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": [
    "src/**/*.ts",
    "src/**/*.js"
  ]
}
